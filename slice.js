/**
 * 2017-09-14  Shi Mengyv
 */

var arr = ['a', 'b', 'c', 'd', 'e', 'f', 'g'];

console.log(`arr.slice(0) => [${arr.slice(0)}]` );

console.log(`arr.slice(-1) => [ ${arr.slice(-1)} ]` );

console.log(`arr.slice(-2, -1) => [ ${arr.slice(-2, -1)} ]` );

console.log(`arr.slice(-2) => [ ${arr.slice(-2)} ]` );

console.log(`arr.slice(0, -2) => [ ${arr.slice(0, -2)} ]` );

console.log(`arr.slice(-4, 1) => [ ${arr.slice(-4, 1)} ]` );

console.log(`arr.slice(8) = [ ${arr.slice(8)} ]` );
